<%= form_tag(:controller => "list_items", :action => "destroy", :method => :delete ) do %>
  <div><%= hidden_field_tag :id, @id %></div>
  <br>
 <%= submit_tag "Delete", class: "btn btn-large" %>
<% end %>

